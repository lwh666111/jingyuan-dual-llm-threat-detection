# 更新日志（2026-04-04 ~ 2026-05-04）

## 2026-05-09

- 修复：前端管理员“系统配置”页面恢复并增强（运行环境一键检查、模型列表刷新、网卡列表刷新、LLM 模型选择、抓包网卡选择）。
- 修复：钓鱼网站检测插件在 token 过期/无效时，后端返回明确错误信息（含过期时间与上游错误透传），前端可直接展示可读提示。
- 优化：抓包网卡自动选择策略优先真实业务网卡，降低误选虚拟网卡/回环网卡概率。
- 优化：`deploy/start_all_nodocker.ps1` 支持本地离线安装包优先安装（Wireshark/Npcap/Ollama），并增强 MySQL 本机自动安装与连通检测。
- 变更：`deploy/.env.nodocker.example` 默认模型调整为 `qwen2.5:3b`，与当前实测环境保持一致。
- 发布：重打安装包 `dist/JingyuanTrafficPipeline_Setup_ManualDeps.exe`（2026-05-09 18:37:30）。

## 2026-05-04

- 修复：`app.py` 读取 `config/db_config.json` 时兼容 UTF-8 BOM（`utf-8-sig`），避免 Windows 下 JSON 解析失败。
- 修复：统一入口默认自动读取 `config/db_config.json`，减少手动参数导致的 MySQL 端口配置错误。
- 修复：抓包配置热更新增加 `capture_interface` 维度，前端改网卡后会自动重启抓包进程生效。
- 新增：管理员系统配置支持网卡下拉选择（`capture_interface`）。
- 新增：管理员运行环境一键检查增加抓包深度检测（tshark、网卡枚举、网卡匹配、短时抓包探测）。
- 新增：运行环境一键检查增加“检测模型文件”检查项，明确提示旧兼容模型文件是否存在。
- 修复：检测链路缺少旧模型文件时，启动日志会输出明确缺失路径，避免“状态正常但无结果”的假象。
- 新增：项目内置旧兼容模型文件：
  - `models/preprocessor.joblib`
  - `models/best_mlp.pth`
- 新增：`test/` 目录，提供靶场一键启停脚本：
  - `test/start_multivuln_lab_4000.ps1`（推荐，多漏洞）
  - `test/start_multivuln_lab_4000.bat`
  - `test/start_login_lab_3000.ps1`
  - `test/stop_target_labs.ps1`

## 2026-04（摘要）

- 完成：`app.py` 一键启动全链路（抓包、检测、LLM、入库、API、前端）。
- 完成：MySQL 三表入库与自动导入守护进程。
- 完成：Flask API（3049）与前端大屏（1145）联调。
- 完成：JWT 登录鉴权、注册、用户中心、管理员配置与用户管理。
- 完成：RAG 知识库增删改查与重建能力。
- 完成：IP 封禁/解封、封禁列表、插件扩展（钓鱼检测、IP分析、本机状态）。
- 完成：Windows 安装包（Inno Setup）与手动依赖安装目录（Ollama/Wireshark/Npcap）。
