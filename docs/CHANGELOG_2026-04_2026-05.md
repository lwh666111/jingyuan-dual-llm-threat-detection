# 更新日志（2026-04-04 ~ 2026-07-01）

## 2026-08-10

- 新增：后端静默快速防御层，在抓包入口对高置信典型攻击执行低延迟确定性判断。
- 新增：命中后由独立有界队列创建 Windows 入站、出站双向封禁规则，抓包线程不等待防火墙命令完成。
- 新增：27 条高置信组合规则，覆盖 SQL 注入、XSS、命令注入、目录穿越、XXE、SSRF、SSTI、反序列化、Log4j/Spring/Fastjson/Shiro 特征和 WebShell 上传。
- 新增：内部审计表 `demo_fast_defense_audit`；该表不暴露前端接口，规则命中不生成前端告警。
- 兼容：所有请求无论是否触发快速封禁，仍进入原有 Detection V2、POC、行为窗口、态势关联和 LLM/RAG 流程。
- 验证：完整 85 项自动化测试通过；正常登录、普通目录访问、正常搜索、普通 SQL 文本、外链和普通 JSON 均不触发快速封禁。

## 2026-07-01

- 重构：完成 Detection V2 分层检测架构，按 `raw_only / candidate / attack_event` 区分原始日志、候选事件和攻击事件。
- 新增：`input/1.1.n.txt` 原始 HTTP 请求/响应全量入库到 `raw_http_logs`，正常访问不再进入大屏告警。
- 新增：V2 `attack_event` 自动同步到 `demo_attack_events`，旧大屏和详情页可实时展示新架构检测结果。
- 新增：管理员详情页“候选事件复核队列”，支持查看候选、提升为攻击事件、忽略并压回原始日志。
- 新增：行为窗口模型 `models/behavior_model_v2.joblib` 与训练脚本 `scripts/train_behavior_model_v2.py`，用于识别暴力破解、扫描、目录探测、高频请求等行为型攻击。
- 优化：Payload 模型补充正常登录 hard negative，融合层收紧“认证接口 + 模型高分”直接升级规则，降低普通账号密码登录误报。
- 验证：Adversarial 样本 Accuracy/Macro F1 均为 `1.0000`；服务器真实可标注样本 Accuracy `0.9982`、Macro F1 `0.9868`。
- 验证：本地烟测确认 3 条 raw 全量入库，SQL/XSS 两条进入攻击事件，正常登录不进入候选/攻击事件；前端无乱码、无控制台错误。
## 2026-06-04

- 发布：服务器验证版安装包，安装器版本号更新为 `2026.06.04`。
- 新增：重新训练 Web 攻击检测模型，增强 SQL 注入、XSS、命令注入、路径遍历、危险文件上传等经典漏洞流量识别。
- 修复：普通用户名密码登录、普通查询等低风险业务流量误判为高危的问题，新增普通认证流量保护规则。
- 新增：异常流量声音提醒，检测到新的高危告警时播放机械音“注意异常流量”。
- 新增：管理员系统配置支持上传/恢复主页背景图，前端大屏可使用自定义背景。
- 新增：多漏洞测试靶场页面增强，便于用户测试 SQL、XSS、命令注入、路径遍历、上传等场景。
- 优化：安装包脚本去除乱码标题，安装器元信息统一为 `Jingyuan AI Threat Perception Platform`。
- 验证：Windows Server 环境完成 MySQL、tshark/Npcap、Ollama、抓包、检测、LLM、入库、API、大屏启动验证。
## 2026-05-09

- 修复：前端管理员“系统配置”页面恢复并增强（运行环境一键检查、模型列表刷新、网卡列表刷新、LLM 模型选择、抓包网卡选择）。
- 修复：钓鱼网站检测插件在 token 过期/无效时，后端返回明确错误信息（含过期时间与上游错误透传），前端可直接展示可读提示。
- 优化：抓包网卡自动选择策略优先真实业务网卡，降低误选虚拟网卡/回环网卡概率。
- 优化：`deploy/start_all_nodocker.ps1` 支持本地离线安装包优先安装（Wireshark/Npcap/Ollama），并增强 MySQL 本机自动安装与连通检测。
- 变更：`deploy/.env.nodocker.example` 默认模型调整为 `qwen2.5:3b`，与当前实测环境保持一致。
- 发布：重打安装包 `dist/JingyuanTrafficPipeline_Setup_ManualDeps.exe`（2026-05-09 18:37:30）。

## 2026-05-04

- 修复：`app.py` 读取 `config/db_config.json` 时兼容 UTF-8 BOM（`utf-8-sig`），避免 Windows 下 JSON 解析失败。
- 修复：统一入口默认自动读取 `config/db_config.json`，减少手动参数导致的 MySQL 端口配置错误。
- 修复：抓包配置热更新增加 `capture_interface` 维度，前端改网卡后会自动重启抓包进程生效。
- 新增：管理员系统配置支持网卡下拉选择（`capture_interface`）。
- 新增：管理员运行环境一键检查增加抓包深度检测（tshark、网卡枚举、网卡匹配、短时抓包探测）。
- 新增：运行环境一键检查增加“检测模型文件”检查项，明确提示旧兼容模型文件是否存在。
- 修复：检测链路缺少旧模型文件时，启动日志会输出明确缺失路径，避免“状态正常但无结果”的假象。
- 新增：项目内置旧兼容模型文件：
  - `models/preprocessor.joblib`
  - `models/best_mlp.pth`
- 新增：`test/` 目录，提供靶场一键启停脚本：
  - `test/start_multivuln_lab_4000.ps1`（推荐，多漏洞）
  - `test/start_multivuln_lab_4000.bat`
  - `test/start_login_lab_3000.ps1`
  - `test/stop_target_labs.ps1`

## 2026-04（摘要）

- 完成：`app.py` 一键启动全链路（抓包、检测、LLM、入库、API、前端）。
- 完成：MySQL 三表入库与自动导入守护进程。
- 完成：Flask API（3049）与前端大屏（1145）联调。
- 完成：JWT 登录鉴权、注册、用户中心、管理员配置与用户管理。
- 完成：RAG 知识库增删改查与重建能力。
- 完成：IP 封禁/解封、封禁列表、插件扩展（钓鱼检测、IP分析、本机状态）。
- 完成：Windows 安装包（Inno Setup）与手动依赖安装目录（Ollama/Wireshark/Npcap）。



